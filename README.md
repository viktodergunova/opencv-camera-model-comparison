# opencv-camera-model-comparison

### Installing Dependencies

To install the required Python packages listed in the `requirements.txt` file, run the following command:

```bash
pip install -r requirements.txt
